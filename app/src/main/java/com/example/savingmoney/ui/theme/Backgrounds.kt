package com.example.savingmoney.ui.theme

import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

val BackgroundGradients = listOf(
    // Xanh nhạt -> trắng
    Brush.verticalGradient(colors = listOf(Color(0xFFF7F9FC), Color(0xFFB2FEFA))),

    // Hồng pastel -> trắng
    Brush.verticalGradient(colors = listOf(Color(0xFFFFF0F5), Color(0xFFFFFAFA))),

    // Xanh lá nhạt -> trắng
    Brush.verticalGradient(colors = listOf(Color(0xFFE8F5E9), Color(0xFFFFFFFF))),

    // Vàng nhạt -> trắng
    Brush.verticalGradient(colors = listOf(Color(0xFFFFFDE7), Color(0xFFFFFBF0))),

    // Tím nhạt -> trắng
    Brush.verticalGradient(colors = listOf(Color(0xFFF3E5F5), Color(0xFFFFFFFF))),

    // Cam nhạt -> trắng
    Brush.verticalGradient(colors = listOf(Color(0xFFFFF3E0), Color(0xFFFFFAF0))),

    // Gradient xanh -> tím nhẹ
    Brush.verticalGradient(colors = listOf(Color(0xFFB2FEFA), Color(0xFF0ED2F7))),

    // Gradient hồng -> cam nhẹ
    Brush.verticalGradient(colors = listOf(Color(0xFFFFC1E3), Color(0xFFFFA07A)))
)
